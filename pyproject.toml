[project]
name = "aurum-terminal"
version = "1.0.0"
description = "MT5 tabanli Turkce piyasa analiz, sinyal ve backtest terminali"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-p no:cacheprovider"

[tool.ruff]
line-length = 100
target-version = "py311"
